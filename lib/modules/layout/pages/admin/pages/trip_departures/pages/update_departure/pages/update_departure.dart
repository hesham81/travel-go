import 'package:flutter/material.dart';

class UpdateDeparture extends StatelessWidget {
  const UpdateDeparture({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
