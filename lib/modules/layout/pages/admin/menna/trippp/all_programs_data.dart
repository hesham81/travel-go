import 'package:flutter/material.dart';

class AllProgramsData extends StatelessWidget {
  const AllProgramsData({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
